{% extends '@lucky/Default/base.html.twig' %}

    {% block main %}

        <div class="container lyrics_container">
            <div class="row justify-content-center">
                <h2 class="col-12 song_title text-center">{{ song.title }}</h2>
                <p class="col-5 song_lyrics">{{ song.lyrics }}</p>
            </div>
        </div>

    {% endblock %}